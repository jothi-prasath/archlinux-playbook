---
- name: Install gnome packages
  pacman:
    name: "{{ gnome_packages }}"
    state: latest
  changed_when: false

- name: Enable GDM service
  service:
    name: gdm
    enabled: true
  changed_when: false